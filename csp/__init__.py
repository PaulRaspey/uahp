"""CSP — portable semantic state (5 vectors) for UAHP agents.

The csp.csp module calls external model APIs and requires the [csp]
extra (groq). It is not imported here so the base install stays light.
"""
